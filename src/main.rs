use clap::{Arg, App}

fn main() {
    let matches = App::new("Calculate File Hash")
        .version("1.0")
        .author("Aklile Tesfaye <astesfaye@dons.usfca.edu>")
        .about("Helps with file integrity, security checks, automated workflows & ensures compatibility across platforms. Vital for verifying the integrity of firmware and cryptographic signatures/keys/certificates.")
  

}
